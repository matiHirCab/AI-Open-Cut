export const SERVER_INSTRUCTIONS =
  "Call editor_get_status, then project_get_state. Make one logical edit at a time and pass the returned expectedRevision; use timeline_batch_edit with resultAlias references when several dependent edits must commit atomically. On REVISION_CONFLICT, re-read project state. Preview with preview_render_range for lightweight cancellable review and preview_render_frame for exact stills before final export; never set overwrite=true without explicit user permission. Poll process-local jobs with job_get_status and cancel eligible work with job_cancel. Overall readiness means the editor is usable; inspect rendering and speech subsystem readiness and their path diagnostics before preview, export, or TTS. Use native solid-color and rectangle items for panels and blank screens. Use asset_delete only for unused logical assets; deletion is undoable and shared hash-addressed media remains while current state or history references it. A failed TTS job may include a short-lived generatedArtifact token; call tts_commit_generated_artifact with the refreshed revision to avoid rerunning synthesis. Call tts_get_status before speech generation and respect its FIFO queue capacity. Imports, exports, fonts, and generated media are limited to configured local directories; URLs and arbitrary scripts are unavailable. Keep edits small and inspect state after consequential changes.";
