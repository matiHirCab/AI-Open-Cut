## 1. Approval and regression baselines

Historical follow-up (2026-09-08): the no-findings verification below did not cover replacement source aliases, full retained geometry budgets or the obsolete flat snapshot. Do not reuse it as evidence for those cases. The [follow-up correction](../2026-09-08-fix-repeater-aliases-and-retained-preflight/proposal.md) tracks fresh regression and verification evidence; the original results below are preserved.

- [x] 1.1 Run `bunx @fission-ai/openspec@1.5.0 validate fix-repeater-expansion-conformance --strict --no-interactive` and correct every artifact error. Passed on 2026-09-07.
- [x] 1.2 Obtain and record explicit user or reviewer approval of `proposal.md`, all three delta specs, `design.md`, and this task list before editing executable implementation files. Approved by the user on 2026-09-07 with “Approve.”
- [x] 1.3 Add failing evaluated-scene tests for root and component-local groups containing direct/nested component instances, nested component repeaters, two independent repeaters, and preserved transform/opacity/clock/bindings/identity facts. Trace: repeaters / Include component descendants in group copies, Repeat each supported source. The regression initially produced 5 layers instead of 9.
- [x] 1.4 Add a failing order regression with at least eleven overlapping equal-z component siblings plus mixed flat/component group descendants, asserting numeric occurrence order and the topmost rendered semantic layer. Trace: repeaters / Preserve deterministic ordering and identity; rendering-export / Preserve visible order with large component subtrees. The regression initially ordered `0-10` before `0-2`.

## 2. Unified editor-core expansion

- [x] 2.1 Change scope evaluation to exclude repeaters from the flat pass, recursively assemble ordinary flat and component occurrences with their source ownership/order metadata, and freeze that same-scope snapshot before any repeater expansion. Trace: repeaters / Deterministic additional-copy semantics.
- [x] 2.2 Replace the split flat/component copy paths with one editor-core repeater expansion pass that selects shape, complete transitive group, or complete resolved component bases without copying another same-scope repeater's generated block. Trace: repeaters / Repeat each supported source, Include component descendants in group copies.
- [x] 2.3 Apply every copy around the persisted source parent's coordinate frame, retaining interval/clip intersection, inherited opacity, local clock, slot/resource bindings, checked arithmetic, occurrence limits, and stable identity semantics. Trace: repeaters / Apply exact interval and opacity behavior; rendering-export / Match all render intents.
- [x] 2.4 Build generated order from the repeater's canonical position, numeric copy index, and source-relative numeric `InstanceOrder`, using identity only as a final tie-breaker; confirm existing simple repeater identities and ordering remain stable. Trace: repeaters / Preserve deterministic ordering and identity.

## 3. Persistence recovery evidence

- [x] 3.1 Extend the pre-journal migration fault matrix with source schema 16 and assert project/history byte identity, retryable later migration, and absence of managed transaction files. Trace: project-persistence / Recover schema 16 before the commit point.
- [x] 3.2 Extend every post-journal publication fault phase with source schema 16 and assert one complete schema-17 current/undo/redo generation plus transaction cleanup after reopen. Trace: project-persistence / Recover schema 16 after every publication phase.

## 4. Renderer and historical evidence

- [x] 4.1 Expand the native repeater golden fixture so a repeated group actually contains a component instance and the greater-than-nine equal-z case has an observable occlusion oracle; compare frame, range, materialized draft, and export. Trace: rendering-export / Match all render intents, Preserve visible order with large component subtrees.
- [x] 4.2 Update the archived `2026-09-07-add-lazy-repeaters/proposal.md` approval section with a dated completion note that references its `tasks.md` contract-review evidence while preserving the original approval chronology.

## 5. Focused verification

- [x] 5.1 Run `cargo test -p opencut-editor-core --test repeaters`, the focused evaluated-scene repeater unit filter, the focused schema-16 recovery tests, and the focused native repeater conformance test with `OPENCUT_GOLDEN_REQUIRED=1` and the repository's pinned FFmpeg/font configuration. Passed 6 integration, 10 focused unit, 2 migration recovery, and 1 required native conformance test.
- [x] 5.2 Run the full native renderer golden/parity gate with required tools and verify frame/range/draft/export remain within the existing SSIM, PCM, and timing tolerances. Passed with required FFmpeg 8.1.2 and the reviewed DejaVu Sans fixture in 475.70 seconds.
- [x] 5.3 Confirm `git diff` contains no changes to public Rust/TypeScript request or response types, MCP schemas/catalogs, `contracts/repeaters-v1.json`, protocol identifiers, error behavior, or `PROJECT_SCHEMA_VERSION`; if any such change becomes necessary, stop and amend/reapprove the OpenSpec artifacts and obtain contract review. The corrective implementation touched only evaluated-scene assembly, core tests/goldens, migration tests, and OpenSpec evidence; pre-existing issue-31 contract edits were preserved unchanged.

## 6. Repository gates and closure

- [x] 6.1 Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace -- --test-threads=1` from the repository root. All passed; workspace tests included 12 desktop tests, 256 passing editor-core unit tests with 6 intentional ignores, all integration suites, and 24 headless protocol tests.
- [x] 6.2 From `apps/agent-bridge`, run `bun run typecheck`, `bun run lint`, `bun run test`, `bun run contracts:check`, `bun run test:integration`, and `bun run test:smoke`. All passed on 2026-09-07: lint covered 63 files, unit tests covered 19 files/387 tests, contract parity covered 6 files/323 tests plus the canonical Rust/Python checks, integration covered 10 tests, and packaged smoke covered 5 tests.
- [x] 6.3 From the repository root, run `bun run apps/agent-bridge/scripts/run-python-tests.ts` and `git diff --check`. Passed on 2026-09-07: the unittest suite ran 10 tests, pytest ran 5 tests, and the diff whitespace check was clean.
- [x] 6.4 Obtain and record explicit user or reviewer approval of the amended proposal, repeater delta, design, and tasks before further executable implementation edits. Approved explicitly by the user on 2026-09-07 with “Approve.”
- [x] 6.5 Add failing evaluated-scene regressions for a hidden `outer -> inner -> 255 groups` source with 254 copies at exactly 65,536 occurrences and one additional ordinary item at 65,537, plus a rich-text slot target inside a locally repeated group whose component is repeated externally. Trace: repeaters / Count nested group source closures transitively; Preserve rich-text bindings through nested repeaters. Before the fix, the 65,537 case returned `Ok`, and local copies retained `Base` instead of the resolved `RedBlue` rich document.
- [x] 6.6 Make group source-closure preflight recurse through each child source while leaving ordinary flat track counting unchanged; verify exact-boundary acceptance, one-over rejection, hidden/inactive enforcement, checked arithmetic, and no partial scene allocation. Trace: repeaters / Lazy bounded occurrence expansion. Implemented by recursing only from `count_source(Group)`; the exact 65,536 case and one-over rejection pass.
- [x] 6.7 Resolve component-instance rich-text documents into the child evaluation scope and apply them to directly owned ordinary text layers before its repeater snapshot; remove the post-recursion depth filter and verify local/outer copies preserve text, rich runs, styles, identity, order, and intervals without leaking across equal IDs in nested scopes. Trace: repeaters / Deterministic additional-copy semantics; Preserve rich-text bindings through nested repeaters. The owning scope now applies resolved documents before identity/snapshot assembly; the local/outer/equal-ID regression passes with four overridden and four isolated nested occurrences.
- [x] 6.8 Re-run the focused evaluated-scene repeater/preflight tests, `cargo test -p opencut-editor-core --test repeaters`, schema-16 recovery tests, and focused required native repeater conformance with pinned FFmpeg/font configuration; record counts and results. Passed on 2026-09-07: 12 focused repeater units (including both new regressions), 6 integration tests, 2 migration recovery tests, and 1 required native repeater test in 24.80 seconds using FFmpeg/FFprobe 8.1.2 and the reviewed DejaVu Sans fixture.
- [x] 6.9 Re-run Rust format, workspace strict Clippy/tests, the full required native renderer golden, all agent-bridge type/lint/unit/contract/integration/smoke gates, hermetic Python tests, and `git diff --check`; confirm no public contract, schema-version, ID-format, error, or persisted-shape changes. Passed on 2026-09-07: format and strict workspace Clippy were clean; workspace tests included 258 passing editor-core units with 6 intentional ignores plus every integration/protocol suite; the required full native golden passed in 269.16 seconds; bridge typecheck, 63-file lint, 387 unit tests, contract parity, 10 integration tests, and 5 packaged-smoke tests passed; Python ran 10 unittest and 5 pytest cases; and `git diff --check` was clean. The corrective edits remain confined to evaluated-scene behavior/tests and OpenSpec evidence, with schema 17, public contracts, ID formats, errors, and persisted shapes unchanged.
- [x] 6.10 Run direct strict validation for the active change and all OpenSpec content, then use `$openspec-verify-change` to reconcile every amended requirement, scenario, design decision, task, test, and implementation. The 2026-09-07 verification evidence was invalidated by a later code review that found recursive preflight before canonical cycle validation and generated-layer materialization before complete expanded-budget projection; it MUST NOT be reused as completion evidence. Fresh verification on 2026-09-07 passed direct strict change validation and full strict validation (24/24 items), then traced all 5 delta requirements and 22 scenarios to implementation and automated evidence with no critical issues, warnings, or suggestions. The review additionally moved repeater source scope/existence/kind checks into the early graph gate; strict core Clippy and the complete core library suite (263 passed, 7 intentional helpers ignored) remained green.
- [x] 6.11 After successful fresh verification, sync all three delta specs, archive with `$openspec-archive-change`, run the archive-only `moon run root:openspec-validate` policy gate, and record the final archive path and policy result without marking this task complete prematurely. Completed on 2026-09-08: all three deltas were synchronized before the change was archived at `openspec/changes/archive/2026-09-08-fix-repeater-expansion-conformance`; the pinned Moon 2.3.3 `root:openspec-validate` target passed 231 policy tests and strict validation of all 23 living specs.

## 7. Safe-recursion and complete-budget correction

- [x] 7.1 Amend the proposal, repeater delta, design, and tasks with the safe-recursion and complete-budget findings; run strict validation for the change and full OpenSpec catalog before requesting approval. Trace: repeaters / Scoped acyclic repeater references; Lazy bounded occurrence expansion. Passed on 2026-09-07: direct strict change validation succeeded and full strict validation passed 24/24 OpenSpec items.
- [x] 7.2 Obtain and record explicit user or reviewer approval of the revised proposal, repeater delta, design, and tasks before editing executable implementation files. Approved explicitly by the user on 2026-09-07 with “Approve.”
- [x] 7.3 Add failing subprocess regressions proving hidden/unused component cycles and repeater-reached group cycles return `INVALID_ARGUMENT` without aborting, while missing references retain `ITEM_NOT_FOUND` and valid maximum-depth graphs remain accepted. Trace: repeaters / Reject cyclic graphs before recursive preflight; Preserve missing-reference and valid-depth behavior. The isolated component/group children both exit successfully with `INVALID_ARGUMENT`; the missing-source regression returns `ITEM_NOT_FOUND`, and the existing depth-16/65,536 boundary remains green.
- [x] 7.4 Reorder canonical project validation ahead of recursive SVG/component/repeater preflights, split scope validation into index/invariant, parent/cycle, and repeater/audio passes, and add active-node guards to every recursive group/component traversal without changing public error codes or precedence. Trace: repeaters / Scoped acyclic repeater references. A graph-only canonical gate now precedes recursive preflight, while full validation retains historical asset/SVG/numeric precedence; component occurrence, clock, evaluation, SVG and audio/group walkers all retain active-node defenses.
- [x] 7.5 Add exact 4,096/4,097 visual-layer regressions, exact/one-over segment and raster/memory accumulator tests, a real failed-expansion integration asserting no generated result, and a case where finite `O^i` becomes non-finite after parent conjugation. Trace: repeaters / Preflight complete expanded visual and geometry budgets; Preflight final composed matrices. Exact 4,096 succeeds, 4,097 fails from the public evaluator before returning a scene, the shared accumulator accepts exact segment/raster caps and rejects one-over, and finite `O^154` with non-finite parent conjugation is rejected.
- [x] 7.6 Replace the deep ordinary-layer snapshot with bounded source indices/metadata, extract one reusable non-publishing occurrence measurement from scene refinement, and implement complete two-pass projection/materialization for all same-scope repeaters with checked arithmetic and unchanged IDs/order/RichText behavior. Trace: repeaters / Lazy bounded occurrence expansion. Each scope now projects lightweight base indices, identities, intervals, composed ancestors and order, preflights all existing/projected shape work with the same measurement used by refinement, and only then clones generated layers.
- [x] 7.7 Re-run focused cycle, occurrence, repeater, RichText, order, limit, schema-16 recovery, and required native renderer tests; record commands, counts, tools, and boundary results. Passed on 2026-09-07: 263 editor-core unit tests with 7 intentional ignores, including both subprocess children and all new budget tests; 6 repeater integration tests; 2 schema-16 recovery filters; strict core Clippy; and 1 required native repeater conformance test in 24.56 seconds using FFmpeg/FFprobe 8.1.2 and the reviewed DejaVu Sans fixture.
- [x] 7.8 Re-run Rust formatting, strict workspace Clippy/tests, full required native golden, all bridge type/lint/unit/contract/integration/smoke gates, hermetic Python tests, and `git diff --check`; confirm schema 17, public contracts, IDs, errors, persistence, and RichText semantics remain unchanged. Passed on 2026-09-07: formatting and strict workspace Clippy were clean; the serial workspace suite passed, including 263 editor-core units with 7 intentional subprocess/helper ignores and the full required native golden; bridge typecheck, 63-file lint, 19 files/387 unit tests, 6 files/323 contract tests plus canonical parity checks, 10 integration tests, and 5 packaged-smoke tests passed; Python ran 10 unittest and 5 pytest cases; and `git diff --check` was clean. The safe-preflight correction changes no schema-17 declaration, public contract, generated-ID format, public error, persisted representation, or RichText semantics.
- [x] 7.9 Perform a completely fresh `$openspec-verify-change`, resolve every discrepancy, and only then complete task 6.10 with new traceable evidence. Completed on 2026-09-07: completeness covered all 34 pre-archive tasks, correctness covered 5/5 requirements and 22/22 scenarios, and coherence matched every design decision and repository pattern. No critical issue, warning, or suggestion remained; tasks 6.11 and 7.10 are intentionally reserved for the archive operation itself.
- [x] 7.10 Sync all three deltas, archive with `$openspec-archive-change`, run `moon run root:openspec-validate` and `git diff --check`, and only then complete task 6.11. Completed on 2026-09-08: repeaters, rendering-export, and project-persistence are synchronized; archive and the pinned Moon policy gate succeeded; the final whitespace check was clean.
