# Conformance verification — 2026-09-08

Workflow: openspec-verify-change. Scope: the approved four requirements and eleven scenarios, with unchanged schema 17, protocol 1, public contracts and persisted formats. Existing issue-31 work is preserved.

## Correctness and coherence

| Requirement | Implementation | Automated evidence |
| --- | --- | --- |
| Complete retained transition fact budgets | Separate retained endpoint counts in InstanceTraversal; projection entries inherit the base count; validate_projection uses checked addition before generated publication, with fresh root/definition domains. Hidden counts never populate visible transition vectors. | projected_transition_budgets_include_copies_and_retained_roles covers reproduced 4,369 excess, exact 4,096/4,097, self roles, root, hidden tracks/repeaters/instances/transitions, hidden transition tracks, clipping, local/exterior nesting and unused definitions. transition_domains_are_independent_and_late_failure_never_clones covers independent sums, reordering and late-domain zero-clone rejection. |
| Effective visual-only repeater sources | validate_parent_graph calls effective-audio validation after components, graph and slot checks. apply_component_slots is non-recursive and shared with resolve_component_slots. EffectiveAudio checks defaults and actual overrides, retains active sets, caches by definition plus default-resolved values, and does not expand repeater copies. | effective_repeater_audio_is_rejected_before_publication covers direct canonical validation/evaluation, defaults, overrides, group descendants, nesting, local/unused definitions, hidden/muted/clipped content and zero generated clones. effective_audio_defaults_overrides_and_cache_are_isolated covers silent defaults replacing authored audio, opposite instances in both orders, missing assets and invalid slot types. Existing subprocess cycle and supported-depth suites pass in focused evaluation. |
| Atomic effective-audio repeater validation | Canonical validation remains shared by timeline mutations, drafts and store reopening; public transports do not classify audio. | effective_audio_batches_drafts_and_reopen_preserve_authoritative_state covers valid literal-ID drafts, bad drafts, batch rollback after an earlier valid edit, stale revision, byte preservation, undo/redo, reopening and corrupted current snapshot rejection without rewrite. |
| Transport parity for effective repeater audio rejection | No production transport changes. Source/packaged shared workflow sends typed batches and drafts unchanged; headless delegates to core. | effective_repeater_audio_rejection_is_atomic_over_headless covers fresh-process batch/draft rejection, stale revisions, reopening and authoritative bytes. verifyEffectiveAudio covers source and packaged batch/draft failures, equivalent state, undo/redo/reopen and successful silent-source preview. Existing repeater aliases and lifecycle remain exercised in the same shared workflow. |

The renderer facade invalid_render_work_preserves_project_and_files now also covers expanded transitions and effective audio using a must-not-run backend and unchanged project/filesystem assertions. No public fixture/catalog, dependency direction or persisted-format change was required.

## Execution evidence

- The first run of both new regression tests failed against the old behavior: the canonical audio validator accepted the source and evaluation accepted the oversized generated transition collection.
- Final focused evaluation: 63 passed, one intentionally ignored subprocess entry executed by its parent. New conformance tests: 4 passed. Component evaluation: 9 passed; template slots: 15 passed; core repeater lifecycle (including the new atomic test) is covered by focused and contract gates.
- Format and strict workspace Clippy passed. Bridge typecheck and lint passed (63 files); unit tests 387, contract TypeScript cases 323 plus native consumers, source integration 10 and packaged smoke 5 passed. Python: 10 unittest plus 5 pytest passed. Strict OpenSpec change/catalog validation: 24 passed; whitespace check passed.
- The first source integration attempt found a fixture-only FFprobe mismatch: the fake tool returned audio alongside a requested v:0 geometry stream. The fixture now honors that selector and supplies a silent test source; complete source integration and packaged smoke then passed. No production fallback or validation weakening was introduced.
- The full required workspace gate exited 0: 12 desktop tests, 269 core units (7 intentional helper/report-only ignores), all integration suites, 26 headless protocol tests and doc tests. The core unit/native phase took 412.32 seconds. Native full golden, focused repeater golden and invalid-work facade tests executed and passed without backend skips. Tools are explicit FFmpeg/FFprobe 8.1.2; OPENCUT_GOLDEN_REQUIRED=1; DejaVuSans SHA-256 ae7b7855e115a5966d8b1b3f80f254ccc117ec86f9965e202ee2940453837280.

## Completeness and final closure

All 18 tasks are complete. Four requirements and eleven scenarios have implementation and automated evidence. No unresolved correctness, completeness or design discrepancy was identified in this audit. The three deltas were synchronized and the change was archived at openspec/changes/archive/2026-09-08-fix-repeater-transition-budget-and-effective-audio with its metadata preserved. Post-archive Moon 2.3.3 root:openspec-validate passed (231 policy tests, 23/23 specs), followed by independent strict catalog validation and git diff --check. The active changes directory contains only archive. No required backend gate was skipped. No critical issues, warnings or suggestions remain in this scoped conformance verification.
