## 1. Approval and canonical evidence

- [x] 1.1 Record explicit approval of proposal, design, delta specs and tasks before implementation; preserve both prior archives and capture the current MCP catalog hash.
- [x] 1.2 Extend the canonical template-slot fixture with the complete closed-record negative matrix and expected structural stages, paths and offending keys (Compare exhaustive closed-record rejection evidence).
- [x] 1.3 Add raw-JSON Rust and bridge fixture consumers with own-key preconditions and rejection assertions for defaults and overrides at every applicable location (Reject unknown fields throughout closed slot records).

## 2. Bridge structural correction

- [x] 2.1 Implement the shared public-Zod wrapper with pre-parse own-key checks, parsed-result delegation, preserved generic types and nested issue paths (Preserve nested validation and schema contracts).
- [x] 2.2 Apply it to definitions, bindings, constraints, the eight-kind value union, rich-text documents/runs and managed-asset references across requests/responses. Retain the open safe override-map parser (Preserve valid records and special map identifiers).
- [x] 2.3 Derive metadata from existing declarations and add exact input/output schema equality and nested request/response error-path assertions; confirm registered MCP schemas without modifying the catalog (Preserve nested validation and schema contracts).

## 3. Transport and documentation evidence

- [x] 3.1 Extend the shared source integration/packaged smoke workflow with canonical malformed standalone and aliased batch requests, checking state, revision and byte-identical project/history rollback (Reject malformed records in real standalone and batch workflows).
- [x] 3.2 Retain and run all-eight-kind, special-ID/prototype, required/default precedence, undo/redo/reopen and group-opacity regression evidence (Preserve valid records and special map identifiers).
- [x] 3.3 Document corrected record closure versus open slot-ID maps and synchronize fixture/consumer ownership evidence without new fields, errors, versions or capabilities (Verify compatibility before completion).

## 4. Required verification

- [x] 4.1 Run `cargo fmt --check --all`, `cargo clippy --workspace --all-targets -- -D warnings` and `cargo test --workspace`. Configure OPENCUT_FFMPEG_PATH and OPENCUT_FFPROBE_PATH to the established target/ffmpeg6/extracted/ffmpeg-6.1.1-full_build/bin tools, OPENCUT_TEST_FONT_PATH to crates/editor-core/tests/fixtures/fonts/DejaVuSans.ttf and RUST_TEST_THREADS=1.
- [x] 4.2 From apps/agent-bridge run `bun run contracts:check`, `bun run typecheck`, `bun run lint`, `bun run test`, `bun run test:integration`, `bun run test:smoke` and `bun run scripts/run-python-tests.ts`.
- [x] 4.3 Run `bunx @fission-ai/openspec@1.5.0 validate fix-closed-template-slot-records --strict` and `git diff --check`; confirm unchanged MCP catalog hash plus actual input/output structural schema parity.
- [x] 4.4 Use openspec-verify-change and record named automated evidence for every delta scenario, exact outcomes and any failed/skipped required check; resolve all mismatches before completion.

## 5. Review and archive

- [x] 5.1 Obtain and record completed review from the designated contract CODEOWNER for final fixtures, consumers and compatibility evidence; artifact approval alone does not satisfy this gate.
- [x] 5.2 Synchronize accepted deltas and archive with openspec-archive-change while preserving both previous archives.
- [x] 5.3 Run the final `bunx @moonrepo/cli@2.3.3 run root:openspec-validate` gate and report completion only if every required check and review has passed.
