## ADDED Requirements

### Requirement: Governed component evaluation activation
A versioned component-evaluation runtime catalog MUST govern root edit payloads, alias locations, timing/coordinate/order rules, bounds, success/failure and schema-13 migration fixtures. Contract ownership MUST list every affected Rust, TypeScript/Zod, headless and MCP consumer; native and structural/semantic parity tests MUST consume canonical evidence. Status MUST advertise additive component_instance_evaluation under protocol 1, retaining existing capabilities and stable error codes/retryability. Historical component-definition and slot capability identifiers MUST retain their original promises; documentation MUST identify evaluation as separately activated. Preparatory motion-graphics records outside this milestone MUST remain fixture-only. Designated CODEOWNER review MUST cover canonical changes and consumers before archival.

#### Scenario: Negotiate and validate runtime support
- **WHEN** clients inspect status or send old requests and new valid/invalid standalone or batch requests
- **THEN** schema 13 and the additive capability are reported, existing requests retain meaning, and every governed consumer agrees on canonical acceptance stage and error semantics
