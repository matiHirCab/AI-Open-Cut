## 1. Bind invalid evidence to its concept

- [x] 1.1 Extend both independent invalid-fixture expectation matrices with the exact concept.
- [x] 1.2 Reject concept relabeling before fixture-specific classification and add mirrored regressions.

## 2. Restore invalid-envelope parity

- [x] 2.1 Reject duplicate invalid layer, mask, and renderer-expression effect definitions in TypeScript before classification.
- [x] 2.2 Scan every Rust invalid mask and mirror generic case-insensitive SVG event-handler detection without new dependencies.
- [x] 2.3 Add mirrored duplicate, non-first-mask, and SVG handler spelling regressions with exact invariant assertions.

## 3. Align catalog limit bounds

- [x] 3.1 Apply the positive JavaScript-safe-integer bound to every limit in both Rust catalog validation layers.
- [x] 3.2 Add table-driven wrapper tests for every limit at the inclusive maximum and at the first rejected integer.

## 4. Document, verify, and close

- [x] 4.1 Update fixture documentation and the living requirement through this change; leave ownership files unchanged.
- [x] 4.2 Run the OpenSpec normalization check and pinned strict validation, `contracts:check`, Rust formatting/Clippy/workspace tests, TypeScript typecheck/lint/unit tests, bridge integration, packaged smoke, and `git diff --check`.
- [x] 4.3 Verify requirements, design, tasks, tests, and code agree; sync and archive only this corrective change after all checks pass.
